<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  

<mapper namespace="com.plugspot.mapper.CityCharge">
	<select id="station" parameterType="int" resultType="CityChargeDTO">
		select * from T_CHARGER_NUM where loc_seq=#{loc_seq}
	</select>
</mapper>